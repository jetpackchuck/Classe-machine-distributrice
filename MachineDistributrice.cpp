#include <iostream>
#include <vector>
#include "MachineDistributrice.h"

using namespace std;

void Machine_distributrice::newMachine() {
	vector<vector<Case>> cases(string, Produit);
	list<Produit> logVentes;
	map<Produit, float> prixProduits;
}
